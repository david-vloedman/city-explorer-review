const cacheManager = (function () {
  checkCache: () => {
    const cache = '';
  };

  saveToCache: () => {

  };

  validateCache: () => {

  };

  return {
    check: checkCache,
    save: saveToCache
  };
})();